# Loop Orchestrator State

This directory stores resumable engineering-loop state. Commit it only when your team wants loop history in version control.

- `config.json`: objective and limits
- `state.json`: current state machine snapshot
- `history.jsonl`: append-only event history
